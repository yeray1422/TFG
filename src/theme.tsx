import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#23b6f6",

    },
    secondary: {
      main: "#fc6902",
    },
  },
});

export default theme;
