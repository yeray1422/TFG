import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#fc6902",
    },
    secondary: {
      main: "#fff",
    },
  },
});

export default theme;
